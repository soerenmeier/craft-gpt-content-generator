{% import '_includes/forms.twig' as forms %}

{{ forms.autosuggestField({
	label: 'ApiKey',
	name: 'apiKey',
	value: settings.apiKey,
	suggestEnvVars: true
}) }}

{# {{ forms.lightswitchField({
	first: true,
	label: 'fieldsEnabled',
	name: 'fieldsEnabled',
	on: settings.fieldsEnabled
}) }}
 #}